import { NgxResizeRightDirective } from './ngx-resize-right.directive';

describe('NgxResizeRightDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxResizeRightDirective();
    expect(directive).toBeTruthy();
  });
});
