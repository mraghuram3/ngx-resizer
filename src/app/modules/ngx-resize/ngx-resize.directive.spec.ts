import { NgxResizeDirective } from './ngx-resize.directive';

describe('NgxResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxResizeDirective();
    expect(directive).toBeTruthy();
  });
});
