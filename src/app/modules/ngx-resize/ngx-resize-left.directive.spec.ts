import { NgxResizeLeftDirective } from './ngx-resize-left.directive';

describe('NgxResizeLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxResizeLeftDirective();
    expect(directive).toBeTruthy();
  });
});
