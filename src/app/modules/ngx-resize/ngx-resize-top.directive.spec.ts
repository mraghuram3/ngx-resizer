import { NgxResizeTopDirective } from './ngx-resize-top.directive';

describe('NgxResizeTopDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxResizeTopDirective();
    expect(directive).toBeTruthy();
  });
});
