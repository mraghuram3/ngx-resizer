import { NgxResizeBottomDirective } from './ngx-resize-bottom.directive';

describe('NgxResizeBottomDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxResizeBottomDirective();
    expect(directive).toBeTruthy();
  });
});
